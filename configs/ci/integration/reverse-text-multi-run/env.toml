# Env server for the multi-run RL integration test: one per orchestrator, each bound at
# its run's base port (the test passes --serve.address).
[env.taskset]
id = "reverse-text-v1"

[env.agent.harness]
id = "null"

[env.agent.runtime]
type = "subprocess"
